# GitHub action to get bundle keys

## usage

```cmd
      - name: Get split key
        uses: aptyInc/action-multi-eks@master
        id: keys
        env:
          REGION: <region>
          BRANCH_NAME: <branch-name>
```
